[package]
name = "video_editor_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

# 速度とファイルサイズを極限まで最適化
[profile.release]
opt-level = 3
lto = true
codegen-units = 1